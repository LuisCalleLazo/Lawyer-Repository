
<div>
    <div>
        <h3 class="font-bold">{{$title}}</h3>
    </div>
    <div class="text-center mt-4">
        <input type="{{$type}}" name="{{$name}}" id="{{$id}}" placeholder="{{$placeholder}}"
        class="rounded py-2 pl-3 w-64 text-black">
    </div>
</div>
